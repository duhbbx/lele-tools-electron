# CRM 模块实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 本地 CRM：客户→干系人/项目树（左导航）+ 参数化 Tab 编辑器 + sqlite 持久化 + 合同文件管理。

**Architecture:** spec 见 `docs/superpowers/specs/2026-06-06-crm-design.md`。三层沿用既有模式：main `db/crmStore.ts`（工厂函数注入 db 句柄，便于 :memory: 单测）→ `ipc/crm.ts` → preload `crm` 桥 → 渲染层 SideNav 树 + 泛化 Tab + 两个编辑器组件。

**Tech Stack:** 既有栈。无新依赖。

**通则：** English commits，NO Co-Authored-By。金额一律分(INTEGER)。每任务结束 `pnpm typecheck && pnpm test && pnpm lint` 全绿再提交。

---

### Task C1: 数据层（schema v2 + crmStore，TDD :memory:）

**Files:**
- Modify: `apps/desktop/src/main/db/schema.ts`
- Create: `apps/desktop/src/main/db/crmStore.ts`, `apps/desktop/src/main/db/crmStore.test.ts`

- [ ] **Step 1: 写失败测试（先于实现）**

`crmStore.test.ts` 用 `new Database(':memory:')`（better-sqlite3 在 vitest node 环境可用）+ `migrate(db)` + `makeCrmStore(db)`。用例（完整写出）：

```ts
import Database from 'better-sqlite3'
import { afterEach, beforeEach, describe, expect, it } from 'vitest'
import { makeCrmStore } from './crmStore'
import { migrate } from './schema'

let db: Database.Database
let store: ReturnType<typeof makeCrmStore>

beforeEach(() => {
  db = new Database(':memory:')
  db.pragma('foreign_keys = ON')
  migrate(db)
  store = makeCrmStore(db)
})
afterEach(() => db.close())

describe('crm clients', () => {
  it('creates, lists, updates, deletes clients', () => {
    const id = store.clients.create({ name: '某公司', type: 'company', note: '' })
    expect(store.clients.list()).toHaveLength(1)
    store.clients.update(id, { name: '改名公司', type: 'company', note: 'x' })
    expect(store.clients.get(id)?.name).toBe('改名公司')
    store.clients.remove(id)
    expect(store.clients.list()).toHaveLength(0)
  })
})

describe('cascade + children', () => {
  it('deleting a client cascades contacts/projects/payments/files', () => {
    const cid = store.clients.create({ name: 'c', type: 'person', note: '' })
    const pid = store.projects.create(cid, '项目A')
    store.contacts.create(cid, '张总')
    store.payments.add(pid, { amountCents: 100_00, paidAt: '2026-06-01', note: '' })
    store.files.add(pid, { name: '合同.pdf', storedPath: 'crm-files/1/合同.pdf', size: 10 })
    store.clients.remove(cid)
    expect(store.contacts.listByClient(cid)).toHaveLength(0)
    expect(store.projects.listByClient(cid)).toHaveLength(0)
    expect(store.payments.listByProject(pid)).toHaveLength(0)
    expect(store.files.listByProject(pid)).toHaveLength(0)
  })
})

describe('project update + payments math source data', () => {
  it('updates full project fields and lists payments', () => {
    const cid = store.clients.create({ name: 'c', type: 'company', note: '' })
    const pid = store.projects.create(cid, 'P')
    store.projects.update(pid, {
      name: 'P1', status: 'active', description: 'desc', serverAddr: '1.2.3.4',
      domain: 'p.example.com', adminUrl: 'https://p.example.com/admin', adminUser: 'root',
      adminPass: 'pw', wxAppId: 'wx1', wxAppSecret: 's', wxPayParams: '[{"k":"mchId","v":"123"}]',
      amountCents: 5000_00, endDate: '2026-12-31',
    })
    const p = store.projects.get(pid)
    expect(p?.amountCents).toBe(5000_00)
    expect(p?.domain).toBe('p.example.com')
    store.payments.add(pid, { amountCents: 1000_00, paidAt: '2026-06-01', note: '首款' })
    store.payments.add(pid, { amountCents: 2000_00, paidAt: '2026-07-01', note: '' })
    expect(store.payments.listByProject(pid).map((x) => x.amountCents)).toEqual([1000_00, 2000_00])
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

`pnpm test -- crmStore` → FAIL（模块/表不存在）。

- [ ] **Step 3: schema.ts 增表**

`migrate()` 的 `db.exec` 里追加（注意既有三表保留）：

```sql
CREATE TABLE IF NOT EXISTS crm_clients (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL, type TEXT NOT NULL DEFAULT 'company',
  note TEXT NOT NULL DEFAULT '', created_at INTEGER NOT NULL
);
CREATE TABLE IF NOT EXISTS crm_contacts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  client_id INTEGER NOT NULL REFERENCES crm_clients(id) ON DELETE CASCADE,
  name TEXT NOT NULL, role TEXT NOT NULL DEFAULT '', phone TEXT NOT NULL DEFAULT '',
  wechat TEXT NOT NULL DEFAULT '', email TEXT NOT NULL DEFAULT '',
  note TEXT NOT NULL DEFAULT '', created_at INTEGER NOT NULL
);
CREATE TABLE IF NOT EXISTS crm_projects (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  client_id INTEGER NOT NULL REFERENCES crm_clients(id) ON DELETE CASCADE,
  name TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active',
  description TEXT NOT NULL DEFAULT '', server_addr TEXT NOT NULL DEFAULT '',
  domain TEXT NOT NULL DEFAULT '', admin_url TEXT NOT NULL DEFAULT '',
  admin_user TEXT NOT NULL DEFAULT '', admin_pass TEXT NOT NULL DEFAULT '',
  wx_app_id TEXT NOT NULL DEFAULT '', wx_app_secret TEXT NOT NULL DEFAULT '',
  wx_pay_params TEXT NOT NULL DEFAULT '[]',
  amount_cents INTEGER NOT NULL DEFAULT 0, end_date TEXT NOT NULL DEFAULT '',
  created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL
);
CREATE TABLE IF NOT EXISTS crm_payments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id INTEGER NOT NULL REFERENCES crm_projects(id) ON DELETE CASCADE,
  amount_cents INTEGER NOT NULL, paid_at TEXT NOT NULL DEFAULT '', note TEXT NOT NULL DEFAULT ''
);
CREATE TABLE IF NOT EXISTS crm_files (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  project_id INTEGER NOT NULL REFERENCES crm_projects(id) ON DELETE CASCADE,
  name TEXT NOT NULL, stored_path TEXT NOT NULL, size INTEGER NOT NULL DEFAULT 0,
  uploaded_at INTEGER NOT NULL
);
```

并确认 `sqlite.ts` 打开库时已有 `db.pragma('foreign_keys = ON')`（Task 5 时已加；没有就补）。

- [ ] **Step 4: crmStore.ts（工厂注入 db）**

形状（行映射 snake→camel；所有 SQL 绑定参数）：

```ts
import type Database from 'better-sqlite3'

export interface CrmClientRow { id: number; name: string; type: 'company' | 'person'; note: string; createdAt: number }
export interface CrmContactRow { id: number; clientId: number; name: string; role: string; phone: string; wechat: string; email: string; note: string; createdAt: number }
export interface CrmProjectRow {
  id: number; clientId: number; name: string; status: 'active' | 'done'
  description: string; serverAddr: string; domain: string; adminUrl: string; adminUser: string; adminPass: string
  wxAppId: string; wxAppSecret: string; wxPayParams: string
  amountCents: number; endDate: string; createdAt: number; updatedAt: number
}
export interface CrmPaymentRow { id: number; projectId: number; amountCents: number; paidAt: string; note: string }
export interface CrmFileRow { id: number; projectId: number; name: string; storedPath: string; size: number; uploadedAt: number }

export function makeCrmStore(db: Database.Database) {
  return {
    clients: {
      list(): CrmClientRow[] { /* SELECT 全表 ORDER BY name */ },
      get(id: number): CrmClientRow | null { ... },
      create(c: { name: string; type: 'company' | 'person'; note: string }): number { /* run().lastInsertRowid */ },
      update(id: number, c: { name: string; type: 'company' | 'person'; note: string }): void { ... },
      remove(id: number): void { ... },
    },
    contacts: {
      listByClient(clientId: number): CrmContactRow[] { ... },
      get(id: number): CrmContactRow | null { ... },
      create(clientId: number, name: string): number { /* 其余字段默认空 */ },
      update(id: number, c: Omit<CrmContactRow, 'id' | 'clientId' | 'createdAt'>): void { ... },
      remove(id: number): void { ... },
    },
    projects: {
      listByClient(clientId: number): { id: number; name: string; status: string }[] { /* 树只要轻量行 */ },
      get(id: number): CrmProjectRow | null { ... },
      create(clientId: number, name: string): number { ... },
      update(id: number, p: Omit<CrmProjectRow, 'id' | 'clientId' | 'createdAt' | 'updatedAt'>): void { /* updated_at = Date.now() */ },
      remove(id: number): void { ... },
    },
    payments: {
      listByProject(projectId: number): CrmPaymentRow[] { /* ORDER BY paid_at, id */ },
      add(projectId: number, p: { amountCents: number; paidAt: string; note: string }): number { ... },
      remove(id: number): void { ... },
    },
    files: {
      listByProject(projectId: number): CrmFileRow[] { ... },
      add(projectId: number, f: { name: string; storedPath: string; size: number }): number { ... },
      get(id: number): CrmFileRow | null { ... },
      remove(id: number): void { ... },
    },
  }
}
```

（`...` 处按 stores.ts 既有写法补全 SQL——prepare + 绑定参数 + camel 映射；这是实现细节非占位，模式在 `apps/desktop/src/main/db/stores.ts` 里已有三个现成范例。）

- [ ] **Step 5: 跑测试确认通过 → 提交**

```bash
pnpm test -- crmStore   # 3 文件用例全过；全量 pnpm test 36 个
git add -A && git commit -m "feat: crm data layer (5 tables + store factory, tdd)"
```

---

### Task C2: shared-types CrmBridge + ipc/crm.ts + preload

**Files:**
- Modify: `packages/shared-types/src/index.ts`, `apps/desktop/src/main/index.ts`, `apps/desktop/src/preload/index.ts`
- Create: `apps/desktop/src/main/ipc/crm.ts`

- [ ] **Step 1: shared-types**

追加（Row 类型与 C1 的 crmStore 同形——在 shared-types 重新声明一份渲染层可见的接口，**不要** import desktop 代码）：

```ts
export interface CrmClient { id: number; name: string; type: 'company' | 'person'; note: string; createdAt: number }
export interface CrmContact { id: number; clientId: number; name: string; role: string; phone: string; wechat: string; email: string; note: string; createdAt: number }
export interface CrmProjectLite { id: number; name: string; status: string }
export interface CrmProject {
  id: number; clientId: number; name: string; status: 'active' | 'done'
  description: string; serverAddr: string; domain: string; adminUrl: string; adminUser: string; adminPass: string
  wxAppId: string; wxAppSecret: string; wxPayParams: string
  amountCents: number; endDate: string; createdAt: number; updatedAt: number
}
export interface CrmPayment { id: number; projectId: number; amountCents: number; paidAt: string; note: string }
export interface CrmFile { id: number; projectId: number; name: string; storedPath: string; size: number; uploadedAt: number }

export interface CrmBridge {
  clients: {
    list(): Promise<CrmClient[]>
    create(name: string, type: 'company' | 'person'): Promise<number>
    update(id: number, c: { name: string; type: 'company' | 'person'; note: string }): Promise<void>
    remove(id: number): Promise<void>
  }
  contacts: {
    listByClient(clientId: number): Promise<CrmContact[]>
    get(id: number): Promise<CrmContact | null>
    create(clientId: number, name: string): Promise<number>
    update(id: number, c: Omit<CrmContact, 'id' | 'clientId' | 'createdAt'>): Promise<void>
    remove(id: number): Promise<void>
  }
  projects: {
    listByClient(clientId: number): Promise<CrmProjectLite[]>
    get(id: number): Promise<CrmProject | null>
    create(clientId: number, name: string): Promise<number>
    update(id: number, p: Omit<CrmProject, 'id' | 'clientId' | 'createdAt' | 'updatedAt'>): Promise<void>
    remove(id: number): Promise<void>
  }
  payments: {
    listByProject(projectId: number): Promise<CrmPayment[]>
    add(projectId: number, p: { amountCents: number; paidAt: string; note: string }): Promise<number>
    remove(id: number): Promise<void>
  }
  files: {
    listByProject(projectId: number): Promise<CrmFile[]>
    /** 弹系统文件选择框 → 拷贝到 userData/crm-files/<projectId>/ → 入库；取消返回 null */
    pick(projectId: number): Promise<CrmFile | null>
    open(id: number): Promise<void>
    remove(id: number): Promise<void>
  }
}
```

加 `crm: CrmBridge` 到 WindowApi。

- [ ] **Step 2: ipc/crm.ts**

`registerCrmIpc()`：每个桥方法一个 `ipcMain.handle('crm:<area>:<op>', ...)`，直通 `makeCrmStore(getDb())`（模块内 lazy 单例化：`let store; function s() { return (store ??= makeCrmStore(getDb())) }`）。文件三件套：

```ts
// pick: dialog.showOpenDialog({ properties: ['openFile'] }) → 取消返回 null；
// 选中：fs.mkdirSync(join(app.getPath('userData'), 'crm-files', String(projectId)), { recursive: true })
// → fs.copyFileSync(src, join(dir, basename(src)))（重名时文件名前加 `${Date.now()}-`）
// → files.add(projectId, { name: basename(src), storedPath: relative(userData, dest), size: statSync(dest).size })
// → 返回完整 CrmFile（files.get(newId)）
// open: shell.openPath(join(userData, row.storedPath))
// remove: 先 fs.rmSync(abs, { force: true }) 再 files.remove(id)
```

main/index.ts 注册 `registerCrmIpc()`。

- [ ] **Step 3: preload 桥**

按既有 store/github 桥风格逐方法 `ipcRenderer.invoke`，类型 `WindowApi['crm']`。

- [ ] **Step 4: 验证 + 提交**

`pnpm typecheck && pnpm test && pnpm lint` 全绿。
`git commit -m "feat: crm ipc bridge with contract file management"`

---

### Task C3: Tab 系统泛化

**Files:**
- Modify: `packages/ui/src/Workspace.vue`, `packages/ui/src/components/ToolTabs.vue`

- [ ] **Step 1: tab 描述符**

Workspace.vue 重构（保持既有行为：去重、拖拽重排、v-show 保活、关闭策略）：

```ts
export interface TabDesc {
  /** 唯一键：tool:<toolId> | crm-contact:<id> | crm-project:<id> */
  key: string
  kind: 'tool' | 'crm-contact' | 'crm-project'
  refId: string
  /** crm tab 的显示名（实体名）；tool 的 title 走 registry 不存这里 */
  title?: string
}
```

- `tabs: ref<TabDesc[]>`；`active: ref<string | null>`（存 key）。
- `openTool(toolId)`（原 open 改名，key=`tool:${id}`）；新增 `openCrm(kind, refId, title)`（key=`${kind}:${refId}`，已存在则激活并更新 title）。
- comps 缓存按 key 不变；crm 两种 kind 映射到静态 import 的 ContactEditor/ProjectEditor（C5/C6 创建；本任务先建两个占位组件 `<div>contact editor</div>` / `<div>project editor</div>` 以保编译，C5/C6 替换）。
- 渲染：`<component :is="resolveComp(tab)" v-bind="tab.kind === 'tool' ? {} : { refId: Number(tab.refId) }" @rename="onTabRename(tab, $event)" />`——crm 编辑器通过 emit `rename` 让 tab 标题跟实体名同步。
- TabDesc 不导出到包外也行，但 SideNav 需要 openCrm —— Workspace 把 `openCrm` 通过模板事件接：SideNav emit `open-crm`，签名 `[kind: 'crm-contact' | 'crm-project', refId: number, title: string]`。

- [ ] **Step 2: ToolTabs.vue 改 props**

`defineProps<{ tabs: { key: string; title: string; icon: string }[]; active: string | null }>()`——由 Workspace 计算 display 数组（tool 走 `toolById(meta).name[locale]/icon`；crm-contact icon 👤、crm-project icon 📁、title 用 desc.title）。事件 payload 由 id 改 key；拖拽逻辑同步改 key。

- [ ] **Step 3: 回归验证 + 提交**

`pnpm typecheck && pnpm test` 绿 + `pnpm dev` 手测：原 16 工具开/关/切/拖拽全部如旧。
`git commit -m "refactor: generalize tab system to descriptor-based tabs"`

---

### Task C4: SideNav CRM 树

**Files:**
- Modify: `packages/ui/src/components/SideNav.vue`, `packages/ui/src/i18n.ts`
- Create: `packages/ui/src/components/CrmTree.vue`

- [ ] **Step 1: CrmTree.vue（独立组件，SideNav 引用）**

状态：`clients: ref<CrmClient[]>`、`expanded: ref<Set<number>>`、每客户的 `contacts/projects` 懒加载缓存 `Map<number, {contacts, projects}>`。
行为：
- 顶部行：`CRM` 标题 + 折叠箭头 + `＋客户` 按钮（inline prompt：用一个小输入行展开输入名称 + 公司/个人 select，回车创建——不要用 window.prompt）。
- 客户节点：展开/收起箭头；hover 显示 × 删除（点了变成"确认删除?"二次点击才删，3 秒超时还原——树上不弹 modal）。展开后两个子目录「干系人」「项目」，各带 `＋` 新建（同 inline 输入行，只要名字，创建后立刻 `emit('open', kind, id, name)` 打开编辑器补全其余字段）。
- 条目节点：点击 `emit('open', kind, id, name)`；hover × 删除同客户节点模式。
- 暴露 `refresh()`（defineExpose）——编辑器改名/删除后 Workspace 调用。
- 全部数据走 `window.api.crm.*`，optional-chaining 容错。

- [ ] **Step 2: SideNav 集成**

工具分类列表下方渲染 `<CrmTree @open="(k, id, name) => emit('open-crm', k, id, name)" ref="crmTree" />`；SideNav 的 defineEmits 加 `'open-crm': [kind: 'crm-contact' | 'crm-project', refId: number, title: string]`；defineExpose 转发 `refreshCrm()`。搜索框只过滤工具（CRM 树不参与搜索，YAGNI）。

- [ ] **Step 3: Workspace 接线**

`<SideNav @open="openTool" @open-crm="openCrm" ref="navRef" />`；编辑器 emit rename/delete 时调 `navRef.value?.refreshCrm()`。

- [ ] **Step 4: i18n keys**

dict 追加：`crm.title` CRM/CRM、`crm.contacts` 干系人/Contacts、`crm.projects` 项目/Projects、`crm.addClient` 新建客户/Add client、`crm.company` 公司/Company、`crm.person` 个人/Person、`crm.confirmDelete` 确认删除?/Confirm?。

- [ ] **Step 5: 验证 + 提交**

`pnpm dev` 手测：建客户→展开→建干系人/项目→Tab 打开占位组件；删除二次确认生效。
`git commit -m "feat: crm tree in side nav"`

---

### Task C5: ContactEditor

**Files:**
- Create: `packages/ui/src/components/crm/ContactEditor.vue`
- Modify: `packages/ui/src/Workspace.vue`（替换占位）

- [ ] **Step 1: 组件**

props `{ refId: number }`；emits `{ rename: [title: string]; removed: [] }`。
onMounted `window.api.crm.contacts.get(refId)` → 表单字段：姓名/职位/电话/微信/邮箱/备注(textarea)。脏标记（任何字段改动 dirty=true）；「保存」按钮（dirty 才可点）→ `contacts.update` → emit('rename', name) → dirty=false；「删除干系人」按钮（danger 样式，confirm 行内二次确认）→ `contacts.remove` → emit('removed')（Workspace 关 tab + refreshCrm）。布局用 `.tool-page` + `.field` 行模式（参照 SettingsDialog 的 field 样式，可在本组件 scoped 重写）。

- [ ] **Step 2: Workspace 替换占位 + removed 处理**

`@removed` → `close(tab.key)` + `navRef.value?.refreshCrm()`；`@rename` → 更新 tab.title + refreshCrm。

- [ ] **Step 3: 验证 + 提交**

手测：编辑保存后树与 Tab 标题同步；删除后 tab 关闭、树刷新。
`git commit -m "feat: crm contact editor"`

---

### Task C6: ProjectEditor（最大组件）

**Files:**
- Create: `packages/ui/src/components/crm/ProjectEditor.vue`, `packages/ui/src/components/crm/SecretInput.vue`, `packages/ui/src/money.ts`, `packages/ui/src/money.test.ts`

- [ ] **Step 1: money 工具 TDD**

```ts
// money.test.ts
import { describe, expect, it } from 'vitest'
import { centsToYuan, yuanToCents } from './money'

describe('money', () => {
  it('round-trips', () => {
    expect(centsToYuan(123456)).toBe('1234.56')
    expect(yuanToCents('1234.56')).toBe(123456)
  })
  it('tolerates messy input', () => {
    expect(yuanToCents(' 1,234.5 ')).toBe(123450)
    expect(yuanToCents('abc')).toBe(0)
    expect(centsToYuan(0)).toBe('0.00')
  })
})
```

失败 → 实现（字符串运算避免浮点误差：去逗号空格 → split('.') → 整数分；centsToYuan 用 `(cents/100).toFixed(2)` 可接受）→ 通过。

- [ ] **Step 2: SecretInput.vue（打码输入，复用于密码/secret/支付参数值）**

props `modelValue: string`；内部 `revealed: ref(false)`；`<input :type="revealed ? 'text' : 'password'">` + 👁 toggle + ⧉ 复制按钮（copyText）。

- [ ] **Step 3: ProjectEditor.vue**

props/emits 同 ContactEditor。五个分区（`<h4>` 分隔，单列表单）：
1. **基本信息**：名称、状态 select(active 进行中/done 已完结)、情况说明 textarea、项目金额（文本框，显示 centsToYuan，保存时 yuanToCents）、结束时间 `<input type="date">`。
2. **部署信息**：服务器地址、域名、后台访问地址、访问用户、访问密码(SecretInput)。
3. **微信**：appId、appSecret(SecretInput)、支付参数：`wxPayParams` JSON 解析为 `{k,v}[]` 行编辑（k 普通 input、v SecretInput、行删除 ×、底部 ＋添加参数），保存时 JSON.stringify。解析失败兜底 `[]`。
4. **收款记录**：表格（金额(元输入)/日期 date/备注/×删除），底部 ＋添加收款（行内三输入+确认）。**即时持久化**（payments.add/remove 直接落库，不走 dirty——收款是流水不是草稿）；表格下方显示 `已收合计 ¥X ／ 未收 ¥Y`（Y = amountCents - sum，负数标红）。
5. **合同**：文件列表（名称/大小 KB/日期 + 打开 + ×删除二次确认），「上传合同」按钮 → `files.pick(refId)`（null=取消）。即时持久化。

分区 1-3 走统一 dirty + 顶部「保存」按钮（sticky 顶栏：项目名 + 保存 + 删除项目）。保存 → projects.update（组装 Omit 对象）→ emit rename。

- [ ] **Step 4: Workspace 替换占位（同 C5 Step 2 模式）**

- [ ] **Step 5: 验证 + 提交**

`pnpm test`（money 2 用例 + 全量绿）+ 手测五分区完整流转（建项目→填全→保存→重开 tab 数据在；收款合计/余额对；上传合同→打开→删除）。
`git commit -m "feat: crm project editor with payments and contract files"`

---

### Task C7: 收尾回归

- [ ] **Step 1: 全量门禁** `pnpm typecheck && pnpm test && pnpm lint` 全绿。
- [ ] **Step 2: 手测回归清单**：16 工具开关切换如旧；CRM 全流程（客户→干系人→项目→收款→合同→删除链）；重启后数据都在；级联删除（删客户）后相关 tab 自动关闭——**注意**：删客户时若其干系人/项目 tab 开着，需要 Workspace 在 refreshCrm 后把指向已删实体的 tab 关掉（编辑器 get 返回 null 时自行 emit removed 兜底——在 C5/C6 的 onMounted 里加：`if (!row) { emit('removed'); return }`，确认已实现）。
- [ ] **Step 3: 文档**：README 功能列表加 CRM 一节（3 行）；`docs/踩坑与要点.md` 补要点（tab 泛化、:memory: 测试模式、文件拷贝存储）。
- [ ] **Step 4: 提交 + push** `git commit -m "docs: crm notes and readme" && git push`

---

## 完成标准

CRM 树全流程可用且重启持久；金额/收款计算准确（分存储）；合同文件拷贝/打开/删除一致；既有 16 工具与 AI 面板零回归；全量门禁绿。
